# Please install "termux" app. first, then enter:
$ pkg upgrade
$ pkg install git make grep
$ hash -r
$ git clone https://github.com/tossug/tossug-arena.git
$ cd tossug-arena
$ make
$ ./tossug-arena
$ ./tossug-arena XiangHalf

# Next time you may need:
$ cd tossug-arena
$ git pull
$ ./tossug-arena XiangQi
